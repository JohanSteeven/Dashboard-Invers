# Healthcare Analytics — Variables de Entorno
# Copiar este archivo a .env y ajustar los valores según tu entorno
# Usado por: src/phase3_load.py, src/pipeline_api.py, docker-compose.yml,
#            scripts/create_metabase_dashboard.py

# ── PostgreSQL connection ──
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthcare_db
DB_USER=postgres
DB_PASSWORD=admin

# ── n8n (Orquestador — Fase 5) ──
N8N_USER=admin
N8N_PASSWORD=admin123


